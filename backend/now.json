{
  "name": "onetap-backend",
  "version": 2,
  "builds": [{
    "src": "*.js", 
    "use": "@now/node"
  }],
  "routes": [{ "src": "/.*", "dest": "/" }],
  "env": {
    "TWILIO_KEY": "@twilio-api-key",
    "AIRTABLE_KEY": "@airtable-api-key"
  }
}